<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_header %>
  </head>
  <body>
    <ul>
      <% for (var key in urls) { %>
        <li><%= urls[key] %> : <a href='#'><%= "Shortened URL"%></a></li>
      <% } %>
      </ul>
  </body>
</html>