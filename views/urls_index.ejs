<!DOCTYPE html>
<html lang="en">
  </head>
  <head>
    <% include ./partials/_header %>
  <body>
    <ul>
      <% for (var key in urls) { %>
        <li><%= urls[key] %> : <a href='#'><%= "Shortened URL"%></a></li>
      <% } %>
      </ul>
  </body>
</html>