<!DOCTYPE html>
<html lang="en">
  </head>
  <head>
    <% include ./partials/_header %>
  <body>
    <ul>
      <% for (var key in urls) { %>
        <center>
          <li><%= urls[key] %> : <a href='/urls/<%= key %>'><%= key %></a></li>
          <form action="/urls/<%= key %>/delete" method="POST">
            <button>Delete</button>
          </form> 
        </center>
      <% } %>
      </ul>
  </body>
</html>