<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
    <ul>
      <% for (var key in urls) { %>
        <li><%= urls[key] %> : <a href='#'><%= "Shortened URL"%></a></li>
      <% } %>
      </ul>
  </body>
</html>