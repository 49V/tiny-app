<!DOCTYPE html>
<html lang="en">
  <header>
    <% include ./partials/_header %>
  </header>
  <body>
    <center>
      <h1>shortURL: <%= shortURL %> </h1> 
      <form action="<%= shortURL %>" method="POST">
        <input type="url" name="longURL" placeholder="<%= longURL %>" required>
        <button>Update</button>
      </form>
    </center>
  </body>
</html>
